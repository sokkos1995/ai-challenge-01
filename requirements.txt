# SSL CA bundle for HTTPS to LLM APIs when system certs are missing (used by app/agent.py).
certifi==2026.2.25

# Official Python SDK for MCP (Model Context Protocol).
mcp==1.27.0

# Optional: Cursor local agent provider (LLM_PROVIDER=cursor). Requires Python 3.10+.
# cursor-sdk

# Optional: day_38 UI smoke (homeworks/src/day_38_smoke/run_smoke.py).
# playwright

# day_41 aviation dataset build (or: pip install -r homeworks/artifacts/day_41/requirements.txt)
datasets>=2.14.0
